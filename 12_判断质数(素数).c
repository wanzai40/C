💡解答
 
我们来一步步补全这个程序，实现输出3-100中所有素数的功能。
 
 
 
分析素数判断逻辑
 
素数（质数）是指大于1，且只能被1和自身整除的自然数。
对于一个数 n ，我们只需要用 2 到 n-1 的数去试除，如果能被其中任意一个数整除，就说明它不是素数。
 
 
#include<stdio.h>
void main()
{
    int i, n;
    printf("3-100中所有素数如下：\n");
    // 外层循环：遍历3到100的每个数
    for(n = 3; n <= 100; n++)
    {
        // 内层循环：用2到n-1的数试除n
        for(i = 2; i <= n - 1; i++)
        {
            // 如果n能被i整除，说明n不是素数，跳出内层循环
            if(n % i == 0)
            {
                break;
            }
        }
        // 如果内层循环结束后i >= n，说明n是素数
        if(i >= n)
        {
            printf("%d\t", n);
        }
    }
    printf("\n");
}

 
 
 
代码解释
 
-  n%i == 0 ：判断 n 能否被 i 整除，如果能整除，说明 n 不是素数，跳出内层循环。
-  if(i >= n) ：内层循环结束后，如果 i 的值大于等于 n ，说明 2 到 n-1 都不能整除 n ， n 是素数，就输出它。
 
 
 
最终补全的空为： n%i == 0  ✅
 
 
 
要不要我帮你优化这段代码，让它的运行效率更高？比如把内层循环的上限改成 sqrt(n) 。

